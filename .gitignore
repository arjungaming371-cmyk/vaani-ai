# dependencies
node_modules/
server/node_modules/
server/stt-service/venv/

# builds
.next/
out/
*.tsbuildinfo

# env & secrets — NEVER commit these
.env
.env.local
.env.production

# WhatsApp session data (contains your logged-in session!)
server/.wwebjs_auth/
server/.wwebjs_cache/

# logs & OS junk
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# STT model cache
server/stt-service/models/
